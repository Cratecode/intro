// First, let's get some inputs.
const num1 = "57.5";
const num2 = "901";

// Now, convert them to numbers.

// Now, let's create a new number from their sum.

// Finally, output them to the console. We should see 958.5 as a result.
