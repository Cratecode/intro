{
    "validator": {
        "requireRun": true,
        "validate": [
            {
                "_comment": "An if statement should be used.",
                "file": "index.js",
                "regex": "/if\\s*\\(/",
                "message": "Make sure you use an if statement."
            },
            {
                "_comment": "An if else statement should be used.",
                "file": "index.js",
                "regex": "/if\\s*else\\s*\\(/",
                "message": "Make sure you use an if else statement."
            }
        ]
    }
}
